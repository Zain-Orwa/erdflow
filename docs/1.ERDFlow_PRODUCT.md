# ERDFlow

## Product Constitution

**Working Product Name:** ERDFlow

**Product Type:** Standalone Visual Database Design Desktop Application

**Core Principle:**

> Draw once, progressively refine.

---

# 1. Product Definition

## 1.1 Product Purpose

ERDFlow is a standalone, professional, cross-platform desktop application
for designing, understanding, refining, transforming, and working with
database models.

The application provides one continuous database-design workflow:

Conceptual ERD
    ↓
Relational Schema
    ↓
Physical / Table Design
    ↓
SQL
    ↓
Data

These stages are different representations of the same underlying
database project.

The user should not need to redraw the same database separately at
every stage.

A user can begin with a high-level conceptual database model and
progressively add more detail until the design becomes an
implementation-ready database.

ERDFlow is desktop-first and should provide a fast, responsive, clean,
professional, intuitive, and native-feeling experience on:

- macOS
- Linux
- Windows

ERDFlow must remain an independent product.

Its architecture should maintain clean boundaries so its database-design
capabilities can potentially be reused as a component of a larger system
in the future without coupling the current product to that system.

---

# 2. Problem Statement

Database design normally progresses through several levels:

Conceptual ERD
    ↓
Relational Schema
    ↓
Physical / Table Design
    ↓
SQL
    ↓
Data

Many existing database-design tools focus strongly on only one or two
of these stages.

For example, one application may be good for Conceptual ERD drawing,
while another is needed for relational schemas, physical table design,
SQL generation, or working with data.

This fragments the database-design process.

Users may need to:

- recreate the same database in multiple tools,
- manually transfer information between stages,
- repeat work when the model changes,
- maintain several disconnected representations,
- manually verify consistency,
- and switch between different applications.

ERDFlow aims to solve this by keeping the database design inside one
project and allowing the same underlying model to progressively evolve
from a conceptual design toward an implementation-ready database.

The goal is not simply:

Conceptual Tool
+
Schema Tool
+
Table Tool
+
SQL Tool
+
Data Tool

The goal is:

One Database Project
        ↓
Conceptual
        ↓
Schema
        ↓
Tables
        ↓
SQL
        ↓
Data

---

# 3. Target Users

ERDFlow should be useful to both people learning database design and
people using database modeling professionally.

Target users include:

- students learning database design,
- teachers explaining ER modeling,
- individual developers,
- software engineers,
- database designers,
- database engineers,
- data architects,
- software architects,
- development teams,
- organizations designing internal database systems.

The interface should remain understandable for beginners while exposing
advanced functionality when required.

Advanced functionality should not make the basic modeling experience
unnecessarily complicated.

---

# 4. Product Goals

ERDFlow should:

1. Make conceptual database modeling easy to understand and use.
2. Support professional database-design concepts.
3. Keep different database-design stages connected.
4. Reduce repeated manual work.
5. Support progressive refinement.
6. Perform known database transformations deterministically.
7. Allow users to review and modify generated results.
8. Protect manual work when upstream or downstream models change.
9. Support controlled synchronization between modeling levels when a meaningful equivalent exists.
10. Import existing database information.
11. Generate implementation-ready SQL.
12. Provide a live, spreadsheet-like Data Workspace for viewing, sorting, filtering, searching, and editing rows.
13. Remain responsive with large projects.
14. Work consistently across macOS, Linux, and Windows.
15. Remain fully useful without AI or cloud services.
16. Maintain clean product boundaries so capabilities can be reused later.

---

# 5. Product Non-Goals

ERDFlow V1 is NOT:

- an ERP system,
- a company-management platform,
- an HR system,
- a finance system,
- a CRM,
- an autonomous-agent platform,
- a cloud collaboration platform,
- an AI-first product,
- a web application,
- a distributed database server,
- a database engine,
- or a replacement for an actual DBMS.

ERDFlow designs and works with database structures.

It does not become the larger Exentury platform during current development.

---

# 6. Core Product Workflow

The primary ERDFlow workflow is:

External Information
        │
        ├───────────────► Import
        │
        ▼
Conceptual ERD
        ↓
Relational Schema
        ↓
Physical / Table Design
        ↓
SQL
        ↓
Data

Users can either:

- start manually from a Conceptual ERD,
- or import existing structured information.

The stages should remain connected to the same project.

---

# 7. Progressive Refinement

ERDFlow follows:

> Draw once, progressively refine.

For example, a user may begin with:

Student --- Enrolled --- Course

Then add cardinality:

Student M:M Course

Then add participation:

Student = Total Participation

Course = Partial Participation

Then add attributes to the relationship:

RegistrationDate
Grade
Attempt

Then convert the relationship into an associative entity if required.

The user should not need to delete and rebuild the model at every step.

Existing concepts should become progressively richer.

---

# 8. Project Structure

One database design should normally exist inside one ERDFlow project.

Working project extension:

.erdx

Example:

university.erdx

A project may contain:

University Project

├── Conceptual ERD
│   ├── Page 1
│   ├── Page 2
│   └── Page 3
│
├── Relational Schema
├── Table Design
├── SQL
└── Data

Large Conceptual ERDs may use multiple pages.

The pages remain part of the same conceptual model.

The project format must be versioned so it can evolve safely over time.

---

# 9. Desktop Experience

ERDFlow is desktop-first.

The experience should feel:

- fast,
- responsive,
- professional,
- clean,
- intuitive,
- familiar,
- native-feeling.

The application should use an Office-like interaction philosophy without copying Microsoft branding.

A possible application structure is:

File
Home
Insert
Design
View
Convert
AI
Export
Help

The exact ribbon contents change depending on the active workspace.

The overall layout should remain familiar:

----------------------------------------------------------
Top Menu / Ribbon
----------------------------------------------------------
Explorer | Main Workspace / Canvas | Properties
----------------------------------------------------------
Conceptual | Schema | Table | SQL | Data
----------------------------------------------------------
Status | Zoom | Messages | Readiness
----------------------------------------------------------

The bottom modeling stages behave conceptually like sheets inside one workbook.

---

# 10. Conceptual ERD

The first database-design level is the Conceptual ERD.

ERDFlow uses Chen-style notation.

Basic symbols:

Rectangle = Entity

Oval = Attribute

Diamond = Relationship

Underlined Attribute = Key Attribute

Plain Line = Connector

Normal Chen relationship connectors should not use arrowheads.

Example:

[Customer] ---- ◇ Subscribe ◇ ---- [Mobile]

---

# 11. Relationship Cardinality Rule

Relationship ratios must appear near the entity to which they belong.

Correct:

[Customer]  1 ---- ◇ Subscribe ◇ ---- M  [Mobile]

The `1` belongs visually near Customer.

The `M` belongs visually near Mobile.

ERDFlow should support:

1:1

1:M

M:1

M:M

The labels must not float randomly in the middle of the connector.

This is a required visual behavior of ERDFlow.

---

# 12. Conceptual ERD Modes

The Conceptual ERD uses one underlying conceptual model with two working modes:

1. Basic / Learning
2. Convertible / Engineering

These are not separate file formats.

They are different ways of working with the same conceptual model.

---

# 13. Basic / Learning Mode

Basic / Learning Mode focuses on understanding database modeling without implementation clutter.

It is particularly suitable for:

- students,
- teachers,
- database courses,
- beginners,
- conceptual modeling exercises.

It should support:

- Entity
- Attribute
- Relationship
- Connector
- Key Attribute
- 1:1
- 1:M
- M:1
- M:M
- Total Participation
- Partial Participation
- Generalization
- Specialization
- ISA
- Associative Entity
- Weak Entity
- Comments
- Text
- Optional visual images/icons

The diagram should remain visually clean.

---

# 14. Convertible / Engineering Mode

Convertible / Engineering Mode uses the same Conceptual ERD but stores additional structured information required for reliable conversion.

Example attribute:

Name: StudentID

Logical Type: Integer

Identifier: Yes

Unique: Yes

Required: Yes

Description: Unique student identifier

Comment: Permanent identifier assigned when the student registers.

Advanced information does not need to clutter the diagram.

It may appear in the Properties panel.

Optional display settings may include:

- Show Data Types
- Show Comments
- Show Participation
- Show Advanced Metadata

---

# 15. Logical Data Types

Convertible Conceptual Mode should use portable logical data types.

Initial logical type family:

- Text
- Integer
- Decimal
- Boolean
- Date
- DateTime
- Binary
- UUID

Conceptual modeling should not immediately force the user to select a specific DBMS physical type.

Example:

Conceptual:

Text(100)

Later:

PostgreSQL → VARCHAR(100)

MySQL → VARCHAR(100)

SQL Server → NVARCHAR(100)

SQLite → TEXT

Physical DBMS-specific types belong primarily to Table Design.

---

# 16. Attribute Types

The Conceptual ERD should support:

## Normal Attribute

Example: Name

## Key Attribute

Example: StudentID

## Composite Attribute

Name
├── FirstName
├── MiddleName
└── LastName

## Multivalued Attribute

Example: PhoneNumber

## Derived Attribute

BirthDate
    ↓
Age

---

# 17. Generalization and Specialization

ERDFlow should support Conceptual ERD inheritance structures.

Example:

        Person
           |
          ISA
         /   \
        /     \
   Student   Employee

Supported concepts should include:

- Supertype
- Subtype
- ISA
- Generalization
- Specialization
- Disjoint
- Overlapping
- Total Specialization
- Partial Specialization

Possible properties include:

Type: Specialization

Supertype: Person

Subtypes: Student, Employee

Constraint: Disjoint / Overlapping

Completeness: Total / Partial

---

# 18. Participation

ERDFlow must support:

- Total Participation
- Partial Participation

The user should be able to refine participation without rebuilding the relationship.

Example:

Student participation: Total

Course participation: Partial

Chen visual notation should clearly communicate the difference.

---

# 19. Cardinality and Ordinality

Simple conceptual relationships may use:

1:1

1:M

M:1

M:M

More detailed relationships may later use:

0..1

1..1

0..M

1..M

The same relationship should become more detailed rather than requiring the user to recreate it.

---

# 20. Associative Entities

Associative entities must be supported.

Example:

Student --- Enrolled --- Course

If Enrolled needs its own properties:

- RegistrationDate
- Grade
- Attempt

the application should support an operation conceptually equivalent to:

Convert to Associative Entity

The resulting associative entity should preserve the meaning of the original relationship.

---

# 21. Weak Entities

Weak entities must be supported using familiar Chen notation.

ERDFlow should understand:

- Weak Entity
- Owner / Strong Entity
- Identifying Relationship
- Partial Key / Discriminator

Example:

Employee
   |
  Has
   |
Dependent

Dependent depends on Employee for its existence and identification.

---

# 22. Comments and Descriptions

Users should be able to store comments or descriptions on:

- Entities
- Attributes
- Relationships
- Schema Relations
- Tables
- Columns

Example:

Entity: Student

Comment: Represents a registered university student.

Comments should remain attached to the semantic database model and, where applicable, travel forward into later stages.

---

# 23. Images and Icons

Conceptual diagrams may support:

Insert → Image

and potentially:

Insert → Icon

Images and icons are visual aids only.

They are not semantic database objects.

Example:

A Customer entity may display a customer icon.

However:

Deleting the icon must NOT delete the Customer entity.

Visual decoration and database meaning must remain separate.

---

# 24. Conversion Readiness

Convertible Conceptual Mode should provide a readiness system before conversion.

Example:

Schema Readiness: 87%

Ready:

✓ All entities have identifiers
✓ Relationships defined
✓ Participation defined
✓ Attribute types defined

Needs Attention:

⚠ Course.Description has no logical type
⚠ Employee specialization mapping is unresolved

Possible actions:

- Fix Issues
- Review
- Convert Anyway

Readiness should guide the user without unnecessarily preventing them from continuing.

---

# 25. Conceptual to Relational Schema Conversion

When the Conceptual model is sufficiently defined, the user should be able to choose:

Convert → Relational Schema

Known database transformations should be deterministic.

Examples:

Strong Entity → Relation

1:M → Foreign Key on the M side

M:M → Junction Relation

Multivalued Attribute → Separate Relation

Associative Entity → Relation

Weak Entity → Owner key combined with weak-entity identification

Generalization / Specialization → Selected relational mapping strategy

Known database rules should not require AI.

---

# 26. Relational Schema Workspace

After conversion, ERDFlow creates or updates the Relational Schema workspace.

Example:

Relations

├── Student
├── Course
└── Enrollment

A relation may expose:

Student
├── StudentID
├── FirstName
├── LastName
├── Email
└── DateOfBirth

The generated schema remains editable.

Users may:

- rename relations,
- rename attributes,
- modify keys,
- add attributes,
- split structures,
- add technical attributes,
- add comments,
- modify schema structures.

Generated does not mean locked.

---

# 27. Change, Diff, and Controlled Synchronization

The Convertible Conceptual model and Relational Schema are connected representations of the same database project, but they are not identical levels.

Changes may originate from either level.

ERDFlow must protect user modifications and must not silently overwrite one level because another level changed.

## 27.1 Conceptual → Schema

Example:

1. The user creates a Convertible Conceptual ERD.
2. ERDFlow generates the Relational Schema.
3. The user manually modifies the Schema.
4. Later, the user adds DateOfBirth to Student in the Conceptual model.

ERDFlow should detect the difference and show something similar to:

Conceptual model changed.

Schema differences:

+ Student.DateOfBirth

[ Review Changes ]
[ Apply ]
[ Ignore ]

Applying the change must preserve unrelated manual Schema modifications.

## 27.2 Schema → Conceptual

When the user modifies the Relational Schema, ERDFlow should determine whether the modification has a meaningful equivalent in the Convertible Conceptual model.

Example:

Schema change:

Student.Name
→ Student.FullName

ERDFlow should ask something similar to:

This modification can also be represented in the Conceptual model.

Do you want this modification to appear in the Conceptual ERD too?

[ Yes ]
[ No ]

If the user chooses Yes:

1. ERDFlow applies the Schema modification.
2. ERDFlow updates the corresponding Conceptual element.
3. Both views remain synchronized for that change.

If the user chooses No:

1. ERDFlow applies the Schema modification.
2. The modification remains only in the Relational Schema.

## 27.3 Synchronization Boundary

Not every Relational Schema change belongs in the Conceptual model.

Implementation-oriented or technical changes may belong only to the Schema or Physical model.

Examples may include:

- technical foreign-key attributes,
- surrogate keys introduced only for implementation reasons,
- implementation-only helper attributes,
- indexes,
- database-specific constraints,
- physical data-type decisions.

ERDFlow should offer upstream synchronization only when a valid semantic equivalent exists at the higher modeling level.

If the relationship between the two levels is ambiguous, the user should review the change rather than ERDFlow guessing.

The synchronization principle is:

> Detect → Explain → Ask → Apply

ERDFlow must not silently synchronize destructive, ambiguous, or meaning-changing modifications.

---

# 28. Physical / Table Design

The Physical / Table Design stage contains implementation-specific database information.

Examples:

StudentID INT

FirstName VARCHAR(50)

Email VARCHAR(255)

Table Design should support:

- Columns
- Physical Data Types
- Length
- Precision
- Scale
- Primary Keys
- Foreign Keys
- NOT NULL
- UNIQUE
- CHECK
- DEFAULT
- Auto Increment / Identity
- Indexes
- Database-specific options
- Comments

This is where logical database concepts become physical implementation details.

---

# 29. Database Dialects

ERDFlow should be designed to support multiple database systems.

Potential supported systems include:

- PostgreSQL
- MySQL
- SQLite
- SQL Server
- Oracle

The product must not assume that every user targets the same database.

The exact initial V1 dialect subset will be confirmed before SQL generation implementation begins.

---

# 30. SQL Generation

The Physical / Table Design should generate real SQL.

Example:

CREATE TABLE Student
(
    StudentID INT PRIMARY KEY,
    FirstName VARCHAR(50) NOT NULL,
    LastName VARCHAR(50) NOT NULL,
    Email VARCHAR(255) UNIQUE
);

The SQL workspace should eventually support:

- SQL Preview
- Database / Dialect Selector
- Regenerate
- Copy SQL
- Export `.sql`
- Syntax Highlighting
- Validation

---

# 31. Data Workspace and Live Data Grid

The Data Workspace provides a live, spreadsheet-like view of the rows belonging to a table.

A user should be able to select a table and inspect its rows without leaving the ERDFlow project.

Example:

Student

| StudentID | Name  | CreatedAt        |
|-----------|-------|------------------|
| 1         | Anna  | 2026-08-30 10:15 |
| 2         | Ahmed | 2026-08-30 09:40 |
| 3         | Maria | 2026-08-30 11:05 |

The Live Data Grid should support:

- Add Row
- Edit Row
- Delete Row
- Sort by a selected column
- Sort ascending
- Sort descending
- Filter
- Search
- Refresh
- Import
- Export
- Validation against table constraints

A user should be able to sort the visible rows by any sortable column.

For example, if the table contains a `CreatedAt` column, the user may sort the rows from oldest to newest or newest to oldest.

Example:

Sort: CreatedAt ↓

| StudentID | Name  | CreatedAt        |
|-----------|-------|------------------|
| 3         | Maria | 2026-08-30 11:05 |
| 1         | Anna  | 2026-08-30 10:15 |
| 2         | Ahmed | 2026-08-30 09:40 |

Sorting changes the current displayed order of rows.

It does not redefine a permanent physical row order inside the database.

Creation-time sorting is available only when the data model contains a column that stores creation time, such as `CreatedAt`.

A possible Data Workspace layout is:

┌──────────────── Student ─────────────────────────┐
│ Filter...                 Sort: CreatedAt ↓      │
├────┬────────────┬───────────┬────────────────────┤
│ ID │ FirstName  │ LastName  │ CreatedAt          │
├────┼────────────┼───────────┼────────────────────┤
│ 21 │ Maria      │ Garcia    │ 2026-08-30 11:05   │
│ 15 │ Anna       │ Smith     │ 2026-08-30 10:15   │
│  8 │ Ahmed      │ Ali       │ 2026-08-30 09:40   │
└────┴────────────┴───────────┴────────────────────┘

[ Add Row ] [ Edit ] [ Delete ] [ Refresh ]

The Data Workspace remains part of the same ERDFlow project.

## 31.1 Future Connected Live Data

When ERDFlow later supports direct database connections, the Live Data Grid may display records from a connected database.

Conceptually:

Actual Database
      ⇅
ERDFlow Live Data Grid

Connected-data functionality may later support:

- refresh from the database,
- controlled row insertion,
- controlled row editing,
- controlled row deletion,
- filtering and sorting,
- validation before sending modifications,
- clear indication of unsaved or unapplied changes.

Direct database editing must not be introduced casually.

When implemented, ERDFlow should clearly distinguish between:

- local/sample project data,
- imported data,
- live data from a connected database.

---

# 32. File Import

File → Import is a first-class product capability.

It must not depend on AI.

The deterministic import process should conceptually behave like:

External File
    ↓
Inspect
    ↓
Parse
    ↓
Validate
    ↓
Preview
    ↓
Mapping / Options
    ↓
Import

Imports should not blindly modify a project.

The user must be able to inspect meaningful changes before they are applied.

---

# 33. Open vs Import

Open and Import are different product operations.

## Open

File → Open

opens a native ERDFlow project.

Example:

university.erdx

## Import

File → Import

brings external information into a project.

Examples:

- SQL
- CSV
- JSON
- spreadsheet data
- database metadata

Therefore:

File
├── New
├── Open
├── Save
├── Save As
├── Import
├── Export
└── Close

Native project opening and external-data importing must remain separate concepts.

---

# 34. Import Targets

Importing a file does not always mean importing it into the same stage.

Possible targets include:

Import Source
     │
     ├── Data
     ├── Physical / Table Model
     ├── Relational Schema
     └── Conceptual ERD Proposal

Examples:

CSV → Data

CSV → Infer basic table structure

SQL → Physical Tables

SQL → Relational Schema

Structured Data → Modeling Proposal

The user should choose or review the intended target.

---

# 35. V1 Import Formats

For the initial deterministic import system, ERDFlow V1 should begin with:

1. SQL
2. CSV
3. JSON

These formats provide useful coverage while keeping the initial import scope manageable.

Planned after the initial import formats:

- XLSX / Excel
- database metadata exports
- direct database connections
- additional structured formats

Document and image understanding should not be part of deterministic V1 import.

---

# 36. Import Preview

Before meaningful imported information changes the project, ERDFlow should show a preview.

Example:

Import detected:

3 tables
21 columns
4 primary keys
6 foreign keys
2 warnings

Actions:

[ Preview ]
[ Import ]
[ Cancel ]

For CSV:

Detected Columns

student_id → Integer
name       → Text
email      → Text
birth_date → Date

The user should be able to review and correct detected information.

---

# 37. Deterministic vs AI-Assisted Import

These are different capabilities.

## Deterministic Import

Used when structure can be parsed using normal software.

Examples:

- SQL
- CSV
- JSON
- XLSX
- database metadata

Flow:

File
 ↓
Parser
 ↓
Validation
 ↓
Preview
 ↓
User Approval
 ↓
Project

## AI-Assisted Import

Used later when semantic interpretation is required.

Examples:

- PDF requirements
- business documents
- messy spreadsheets
- screenshots
- ambiguous structures

Flow:

Input
 ↓
AI
 ↓
Structured Proposal
 ↓
Core Validation
 ↓
Preview
 ↓
User Review
 ↓
Accept / Modify / Reject

AI is never allowed to silently change the database model.

---

# 38. Export

ERDFlow should support useful project and presentation exports.

Planned exports include:

- Current Diagram → PNG
- Current Diagram → PDF
- Project / Diagrams → PDF
- SQL → `.sql`
- Project → `.erdx`

Additional export formats may be added later.

---

# 39. Touch, Stylus, and Sketch Recognition

ERDFlow may later support touch-screen and stylus interaction.

Potential functionality includes:

User Sketch
    ↓
Shape Recognition
    ↓
Candidate ERD Element
    ↓
User Confirmation
    ↓
ERD Element

Examples:

rough rectangle → Entity

rough oval → Attribute

rough diamond → Relationship

rough line → Connector

This functionality is a future enhancement.

It is NOT required for the first implementation milestone.

Traditional mouse, keyboard, menus, and toolbars must work independently of sketch recognition.

---

# 40. AI Product Boundary

ERDFlow must be fully useful without AI.

AI is an optional later capability.

Potential future AI actions include:

- Generate Conceptual ERD
- Infer Schema
- Infer Data Types
- Review Design
- Suggest Normalization
- Extract Entities
- Classify Attributes
- Explain Model

AI must follow the rule:

> AI proposes. The user decides.

AI-generated structures must be:

- validated,
- previewed,
- reviewable,
- editable,
- accepted or rejected by the user.

ERDFlow should never silently modify a project because an AI model suggested something.

---

# 41. Cross-Platform Requirement

ERDFlow should provide the same primary application experience on:

- macOS
- Linux
- Windows

Projects created on one supported operating system should be portable to the others whenever possible.

The `.erdx` project format should not depend on one operating system.

---

# 42. Performance Experience

Users should experience ERDFlow as responsive and smooth.

Important user-facing expectations include:

- smooth dragging,
- smooth zoom,
- smooth panning,
- responsive selection,
- fast editing,
- reasonable startup time,
- responsive large diagrams,
- useful progress feedback during long operations,
- cancellation of long operations where appropriate.

The product should eventually handle:

- hundreds of entities,
- thousands of attributes,
- large imported files,
- complex schema conversions.

Exact technical performance architecture belongs in `ARCHITECTURE.md` and `SCALE.md`.

---

# 43. V1 Product Scope

ERDFlow V1 should target the complete core database-design workflow without AI, cloud, ERP, or plugin-system complexity.

V1 product capabilities are:

## Desktop Application

- standalone desktop application,
- macOS,
- Linux,
- Windows,
- Office-like workspace,
- Explorer,
- Canvas / Workspace,
- Properties panel,
- stage tabs,
- status and zoom controls.

## Conceptual ERD

- Chen notation,
- Entity,
- Attribute,
- Relationship,
- Connector,
- Key Attribute,
- Composite Attribute,
- Multivalued Attribute,
- Derived Attribute,
- 1:1,
- 1:M,
- M:1,
- M:M,
- participation,
- weak entities,
- identifying relationships,
- associative entities,
- generalization,
- specialization,
- ISA,
- comments,
- text annotations.

## Conceptual Modes

- Basic / Learning
- Convertible / Engineering

## Project

- `.erdx`
- save
- load
- multiple Conceptual pages
- shared project model

## Conversion

- readiness checks,
- deterministic Conceptual → Schema conversion.

## Relational Schema

- generated Schema,
- editable relations,
- primary keys,
- foreign keys,
- comments,
- schema navigation.

## Change Management and Synchronization

- detect upstream changes,
- review differences,
- apply or ignore changes without destructive regeneration,
- detect Schema changes that have meaningful Conceptual equivalents,
- ask whether eligible Schema modifications should also appear in the Convertible Conceptual model,
- never silently propagate ambiguous or implementation-only changes.

## Table Design

- physical types,
- columns,
- constraints,
- indexes,
- defaults,
- database-specific options.

## SQL

- SQL generation,
- preview,
- validation,
- export.

## Data

- Live Data Grid,
- spreadsheet-like row view,
- add/edit/delete rows,
- sort by columns,
- ascending/descending sorting,
- filters,
- search,
- refresh,
- validation,
- import/export.

## Import

Initial deterministic formats:

- SQL
- CSV
- JSON

## Export

- PNG
- PDF
- SQL
- ERDFlow project

---

# 44. Explicitly Deferred From V1

The following are intentionally deferred:

- AI-generated ERDs
- AI-assisted modeling
- training our own AI
- PDF semantic understanding
- DOCX semantic understanding
- screenshot interpretation
- cloud collaboration
- real-time multi-user editing
- web client
- mobile application
- plugin marketplace
- large distributed backend
- enterprise cloud services
- Exentury ERP integration
- autonomous agents
- touch/stylus shape recognition
- advanced plugin ecosystem
- direct live database connections and write-back, unless explicitly moved into V1 later

Deferred does not mean rejected.

It means these capabilities should not distract from building the database-design product correctly first.

---

# 45. Current Development Milestone

Although ERDFlow V1 includes a larger database-design workflow, we will not build all of V1 at once.

The first practical implementation milestone is:

CONCEPTUAL CHEN ERD EDITOR

The first visual goal is:

- create Entity rectangles,
- create Attribute ovals,
- create Relationship diamonds,
- connect objects using plain lines,
- place 1/M labels near their corresponding entities,
- select objects,
- move objects,
- edit objects,
- delete objects,
- zoom and pan,
- use a Properties panel,
- use the Office-like desktop shell.

The first milestone does NOT include:

- SQL generation,
- Data Grid,
- AI,
- cloud,
- plugins,
- Exentury integration.

Those belong to later development stages.

---

# 46. Future Product Evolution

After the deterministic desktop database-design system is strong, ERDFlow may evolve with:

- additional import formats,
- additional SQL dialects,
- database connections,
- reverse engineering,
- richer live-data workflows,
- touch and stylus support,
- sketch recognition,
- AI-assisted modeling,
- specialized database AI,
- collaboration,
- cloud synchronization,
- plugins,
- additional clients,
- larger ecosystem integration.

Future expansion must preserve the usefulness of the standalone application.

---

# 47. Future Exentury Integration

ERDFlow is an independent product.

In the future, its database-design capabilities may become reusable inside the broader Exentury platform.

Potential capabilities exposed to another system could conceptually include:

database.discover_schema()

database.model_conceptual()

database.model_relational()

database.validate()

database.normalize()

database.generate_sql()

database.visualize()

This integration is not part of current development.

ERDFlow should be successful and useful even if this integration never happens.

---

# 48. Product Principles

ERDFlow follows these product rules:

1. Draw once, progressively refine.
2. One project represents the database across its different design stages.
3. Keep conceptual modeling understandable.
4. Advanced capabilities should not clutter basic workflows.
5. Use `M:M` for many-to-many relationship notation.
6. Known database rules should be deterministic.
7. Generated results remain editable.
8. Never destroy downstream manual work without user review.
9. Support controlled synchronization between modeling levels when a meaningful equivalent exists.
10. For eligible Schema → Conceptual changes: Detect → Explain → Ask → Apply.
11. Never silently push technical or ambiguous Schema changes into the Conceptual model.
12. Import is a core capability independent of AI.
13. Imported information must be reviewed before meaningful changes are applied.
14. The Data Workspace should provide a live, sortable, filterable, searchable, editable view of table rows.
15. Sorting a Data Grid changes the view, not the permanent physical database row order.
16. AI is optional.
17. AI proposes; the user decides.
18. Visual decoration must remain separate from semantic database data.
19. Desktop experience comes first.
20. The product must remain cross-platform.
21. The application must remain useful offline and without cloud services.
22. Do not expand ERDFlow into an ERP product.
23. Design the product so future growth remains possible without over-engineering future features today.

---

# 49. Product Success Definition

ERDFlow succeeds when a user can begin with an idea for a database, represent it visually, progressively refine it, convert it into a relational design, define its physical implementation, generate SQL, and work with its data without repeatedly rebuilding the same database in disconnected applications.

The different levels should remain connected while still respecting their different responsibilities.

A user should be able to intentionally propagate meaningful changes between Convertible Conceptual and Relational Schema when appropriate, without losing control over implementation-specific differences.

The Data Workspace should allow the user to inspect and work with table rows naturally through a live grid with sorting, filtering, searching, editing, deletion, and refresh behavior.

The experience should feel like one database design flowing naturally from:

Concept
   ↓
Structure
   ↓
Implementation
   ↓
SQL
   ↓
Data

That flow is the core identity of ERDFlow.
